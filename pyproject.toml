[project]
name = "customer-support-agent"
version = "0.1.0"
description = "Customer support agentic system"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "pydantic-settings>=2.6",
    "openai>=3.6.0",
    "alembic>=1.19.1",
]

[tool.uv]
package = false
